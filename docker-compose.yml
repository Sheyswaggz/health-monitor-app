services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: health-monitor-app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - ./src:/app/src
      - ./server:/app/server
    restart: unless-stopped